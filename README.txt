Haunted Blanket

Stable static site package.

Test locally in VS Code before pushing. If everything looks correct, commit with:

git add .
git commit -m "Code hygiene cleanup"
git push

Important: keep index.html at the repository root for GitHub Pages.
